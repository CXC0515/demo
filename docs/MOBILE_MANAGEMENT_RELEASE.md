# 移动端管理优化与发布记录

- 版本：v1.4
- 日期：2026-09-17
- 状态：已发布，公网和存储验收通过
- 功能 PR：#26、#28、#30、#32、#34；应用生产提交：fd0f8f7cc31168f541ce8d3d237dd311f0484522

## 问题、决策与范围

手机班级详情原先在列表下方展开、座位图被辅助面板挤压、课表保留桌面最小宽度、四象限及标签分类纵向堆叠。目标是完成手机上的查看、定位和轻量编辑闭环，同时保留电脑端效率。用户于本任务批准实施，并明确授权 PR、合并和上线。

本次需要修改前端。最短路径为复用既有业务数据及保存接口，仅调整布局、入口和座位草稿变换。备选的整体缩放会压缩触控区域，因此课表采用紧凑格子和信息重排，座位图保留可读宽度及受控横向滚动。

- ClassManagement：手机卡片省略学期，详情底部上滑；中屏右侧弹层，宽屏详情栏；编辑和班委操作共享原生 dialog。
- StudentManagement / App：增加“查看学生画像”，定位对应班级和学生。
- VirtualClassroom：待安排名单按需打开；点击交换、左右循环移列、电脑指定行列交换；预览、撤销、保存失败保留草稿；保存后清除旧学生详情状态。
- VirtualClassroom v1.1：手机行列步进器对齐，学号排列、左右轮换和待安排收敛为 2×2 操作区；行列坐标与座位轨道对齐；增加座位图放大预览，电脑直接全屏，手机尝试锁定横屏，不支持时显示旋转提示并保留可控滚动。
- ScheduleReminder：紧凑 5/7 天课表、手机 2×2 四象限、更多操作入口，清单布局保留。
- TagManagement：手机四分类同一行，完整分类名在内容标题显示。
- ResponsiveDialog：原生模态焦点、Escape、焦点恢复、安全区和下滑关闭手柄。
- v1.2：座位表“导出包含学号”移入数据与存储设置；学生管理手机工具区收敛为两行；日程四象限恢复完成方框并增加 AI 优先的悬浮创建入口；标签卡收敛为单行；已归档班级进入独立归档盒；学情诊断选项保持单行；AI 批改手机任务操作收入右侧空间。
- v1.3：座位图手机端班级选择、导出和编辑同排，座位图及学情诊断班级选择改为大选项底部面板；日程删除冗余状态行，更多菜单保留隐藏已完成、隐藏已过期和周期任务；标签启停改为明确勾选状态。
- v1.4：资料 PDF 改为 PDF.js canvas 阅读器，补齐电脑和手机翻页、缩放、适应宽度及打开原文件；上传前可按需预览首页；课表班级、资料类型、知识图谱学科和图谱筛选统一为手机大选项面板，课表/日程页签降低高度。

## 验证证据

- 本地及服务器：TypeScript、生产构建通过，classroom 测试 4/4。
- 隔离测试数据、API 全拦截的 Chromium：375×812、390×844、430×932、844×390、768×1024、1024×768、1440×900。
- 通过：指定学生画像跳转、单人换位、左右轮换、行列交换、撤销、保存、失败保留草稿和重试、待安排学生放置；5/7 天课表、四象限、标签同排；弹层返回、Escape、横屏和尺寸切换。
- 桌面快捷操作移入侧栏后，1440×900 的 42 个座位全部可见且页面无横向溢出。
- v1.1 在 390×844 手机竖屏、844×390 手机横屏和 1440×900 电脑端完成真实 Chromium 渲染检查：工具区、坐标、待安排底部面板和放大预览均正常，控制台无错误。
- v1.2 在 390×844 真实 Chromium 中逐页检查学生、班级归档、日程、标签、学情诊断和批改任务，并实际打开归档面板与悬浮创建菜单；1440×900 验证学生表格、诊断工具条和批改任务表格未因手机编排退化。
- v1.3 通过 lint、生产构建，并在 390×844 Chromium 实际打开座位及诊断班级面板、日程更多菜单和标签状态；测试页唯一控制台错误为缺少 favicon。
- v1.4 通过 lint、生产构建；1440×900 与 390×844 Chromium 均实际绘制 PDF canvas，翻到第 2 页正常，手机选择面板可用；测试页唯一控制台错误为缺少 favicon。
- 未验证：iOS Safari / Android 真机、真实软键盘、真实教师登录后的业务验收、200% 浏览器缩放；不把视口模拟等同于真机。
- 已知限制：构建仍提示已有的大包体积；标签管理原有模拟持久化行为未在此项扩展。

- v1.4 上线后：回环及公网 live/ready 均正常，服务 active，当前链接指向 `fd0f8f7`。
- v1.3 上线后：回环及公网 live/ready 均正常，服务 active，发布目录为 `80787d5`。
- v1.2 上线后：回环及公网 live/ready 均正常，发布目录为 `d0dead8`。
- v1.1 上线后：回环及公网 live/ready 均正常，服务 active，发布目录为 `b13be36`。
- v1.0 上线验收：公网 live/ready 均 200，未登录 classes 401；公网 JS/CSS SHA-256 与新发布构建相同；真实浏览器成功打开 HTTPS 登录页。
- 服务器服务 active/running、NRestarts=0；5 个 SQLite integrity_check 均 ok，两个工作区学生各 53 人，班级分别 4/2 个、资料分别 2/0 份，与发布前相同。

## 发布与回滚

发布目标及权威数据目录以 PROJECT_HANDOFF.md 为准。仅更新应用发布目录；无环境变量修改、数据库结构变更或本地数据上传。

Git HTTPS 连接故障时，通过 GitHub Git Data API 上传相同 blob/tree/commit，并验证远端提交与本地 6712ae9 完全相同。PR #26 合并为 9837298；从 GitHub 签名 payload 重建并验证 merge commit SHA，母文件夹 main 快进到该提交。发布包来自母文件夹 main 的 git archive，上传 SHA-256 校验一致。

服务器新目录独立 npm ci 和 build 后原子切换 current，重启 systemd，并检查回环和公网 live/ready。当前发布目录为 `fd0f8f7`，保留 `80787d5`、`d0dead8`、`b13be36` 与 `9837298` 供回滚；历史旧目录 `03b3a0d` 和 `0a7b95f` 已删除。代码可从 GitHub 重建，生产数据目录和备份未修改。

## 修改历史

| 版本 | 日期 | 概要 |
| --- | --- | --- |
| v1.4 | 2026-09-17 | PDF.js 统一电脑与手机资料阅读，增加上传前预览并放大残余手机选择器。 |
| v1.3 | 2026-09-17 | 放大班级选择选项，收敛座位工具行、日程状态与筛选入口，并明确标签启停状态。 |
| v1.2 | 2026-09-17 | 收敛学生、日程、标签、班级归档、学情诊断和 AI 批改的手机信息层级，导出学号偏好移入设置。 |
| v1.1 | 2026-09-17 | 收敛手机座位编辑工具区、对齐行列坐标，增加全屏和手机横屏预览。 |
| v1.0 | 2026-09-17 | 记录已授权方案、前端实现、浏览器和服务器验证及发布边界。 |
